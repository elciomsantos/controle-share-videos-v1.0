module.exports = {
  plugins: {
    "postcss-preset-mantine": {},
    "postcss-simple-vars": {
      // https://mantine.dev/styles/postcss-preset/
      // Mantine breakpoint values used by responsive mixins
      // Ensure only the required media query variables are declared.
    },
  },
};
