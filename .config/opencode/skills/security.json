{
  "name": "Security",
  "type": "skill",
  "description": "Validação de segurança para código WSL2",
  "knowledge": [
    "OWASP Top 10",
    "SQL Injection prevention",
    "XSS/CSRF protection",
    "Path traversal attacks",
    "Command injection",
    "JWT security",
    "CORS configuration",
    "Rate limiting"
  ],
  "rules": [
    "⚠️ CRÍTICO: Nunca usar eval()/exec()/Function()",
    "⚠️ CRÍTICO: Sempre usar parameterized queries",
    "⚠️ CRÍTICO: Sanitizar inputs do usuário",
    "⚠️ CRÍTICO: Validar upload de arquivos",
    "Usar bcrypt/scrypt para senhas (nunca MD5/SHA1)",
    "Tokens JWT com expiração curta",
    "CORS restrito a origens conhecidas",
    "Rate limiting em todas as APIs",
    "Headers de segurança (CSP, HSTS, X-Frame-Options)"
  ],
  "checklist": [
    "Secrets no código? → BLOQUEAR",
    "SQL concatenado? → BLOQUEAR",
    "eval()/exec()? → BLOQUEAR",
    "os.system() com input? → BLOQUEAR",
    "Debug=True em produção? → BLOQUEAR",
    "Senha em log? → BLOQUEAR"
  ]
}